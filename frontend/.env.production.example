# Copy to .env.production and set to your deployed Render backend URL (no trailing slash).
# This value is baked into the static build at `npm run build`.
VITE_API_BASE=https://travel-planner-api.onrender.com
